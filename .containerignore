# Keep the `podman build .` context lean. Exclude heavy build products and outputs that the
# Containerfile never COPYs. The staged TUI binary (image/tui/neural-ice-tui, pre-built from
# ICE-AC1) and all image/**, ota/**, ignition/** inputs are NOT excluded — the build consumes them.
.git
*.img
*.img.zst
*.img.xz
*.sha256
