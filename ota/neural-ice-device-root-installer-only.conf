[Unit]
# This drop-in exists only in the dual-mode installer image.  Autoinstall
# removes it from the staged target before reboot, so installed appliances run
# the base unit normally while a non-installing Live/rescue boot cannot create
# a persistent TPM object.
ConditionKernelCommandLine=neuralice.autoinstall
